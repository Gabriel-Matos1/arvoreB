atualizar o gcc para a versão que suporta o C23: 
sudo apt update
sudo apt install gcc-14 g++-14



struct arvoreB* criarArvoreB(int32_t t_arvore);
    //Gabriel: podemos assumir que a entrada vai ser um int mesmo? No main do professor vai ter validação? 




Nesse comentario do professor para a função de desalocação de arvore entendi que tem 2 structss, struct ArvoreB e struct nodo/*Libera toda a memória alocada. Ou seja, todos os nodos são liberados, além da struct
arvoreB passada como parâmetro.*/